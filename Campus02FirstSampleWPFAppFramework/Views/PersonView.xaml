<Window x:Class="Campus02FirstSampleWPFAppFramework.Views.PersonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Campus02FirstSampleWPFAppFramework.Views"
        mc:Ignorable="d"
        Title="PersonView" Height="450" Width="800" Background="{Binding Stimmungsfarbe}" >
    <UniformGrid Columns="2" Rows="6">
        <TextBlock>Vorname</TextBlock>
        <TextBox Text="{Binding Vorname}"></TextBox>
        <TextBlock>Stimmungsparameter</TextBlock>
        <Slider Minimum="0" Maximum="10" Value="{Binding Stimmungsparameter}"></Slider>
        <TextBlock>Stimmungsparameter</TextBlock>
        <TextBox Text="{Binding Stimmungsparameter}"></TextBox>
        <TextBlock>IstUrlaubsreif</TextBlock>
        <CheckBox IsChecked="{Binding IstUrlaubsreif}"></CheckBox>
        <Image Source="{Binding Bild}" Height="20" />
        <TextBlock Text="{Binding Bild}"></TextBlock>

    </UniformGrid>
</Window>
